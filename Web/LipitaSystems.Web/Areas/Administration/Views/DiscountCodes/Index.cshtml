@model IEnumerable<LipitaSystems.Data.Models.DiscountCode>

@{
    ViewData["Title"] = "Кодове";
}


<br>
<br>
<br>
<section class="mt-5 container">
    <h1>Кодове за отстъпка</h1>

    <p>
        <a class="btn btn-primary" asp-action="Create">Създай нов код</a>
    </p>
    <table class="table table-responsive">
        <thead>
            <tr>
                <th>
                    Код
                </th>
                <th>
                    Процент намаление
                </th>
                <th>
                    Работи ли на вече намалени продукти
                </th>
                <th>
                    Създаден на:
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Code)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DiscountPercentage)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DoesWorkOnDiscountedProducts)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedOn)
                    </td>
                    <td>
                        <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</section>